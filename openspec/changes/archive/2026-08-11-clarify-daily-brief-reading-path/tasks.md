## 1. Editorial contracts and gates

- [x] 1.1 Add backward-compatible brief item editorial fields and pure extraction/validation tests for complete grounded change, implication, uncertainty, and why-now copy.
- [x] 1.2 Strengthen structured-claim eligibility so primary plus independent corroboration is required, context receives no support credit, contradiction blocks, and unusable supporting evidence fails closed.
- [x] 1.3 Add explicit `ready`, `empty`, and `unavailable` public category states and tests that public composition never substitutes seed fallback content.
- [x] 1.4 Add a pure edition-quality receipt and focused tests for valid, partial-empty, synthetic, malformed, unsupported, and unavailable snapshot cases.

## 2. Brief composition and archive behavior

- [x] 2.1 Remove stock, idea, and trend fallback calls from current and precomputed public brief paths while retaining seed utilities for explicit fixtures and tests.
- [x] 2.2 Stop composing public rotating-product and personalized sections for the web edition while preserving dedicated Mentions/Agent Eval routes and backward-compatible authenticated delivery fields.
- [x] 2.3 Apply quality-first category filtering and ranking before a documented high operational safety bound, with no minimum-count relaxation.
- [x] 2.4 Gate `daily_brief_snapshots` writes with the edition receipt so invalid runs leave existing dated records unchanged and valid partial editions preserve explicit empty states.
- [x] 2.5 Update compact delivery/agent-readable rendering as needed so new editorial fields and category states remain backward compatible.

## 3. Reader path and preserved interface refinement

- [x] 3.1 Create the preserve-lane design-workflow receipt and capture the current brief at 390, 768, and 1440 pixels before UI edits.
- [x] 3.2 Make the current Daily Brief the root canonical surface, add a permanent `/brief` compatibility redirect, and preserve `/brief/archive` plus `/brief/<date>` behavior.
- [x] 3.3 Simplify primary navigation to Brief, Signals, Track record, and Sources while keeping Explore and contextual access to the larger product inventory.
- [x] 3.4 Remove the public product picker and spotlight copy; add edition identity, category counts, stable anchors, explicit category states, and a subordinate market-convergence treatment.
- [x] 3.5 Refactor section and item presentation into one continuous ruled editorial ledger with complete inline summaries, restrained evidence metadata, readable responsive behavior, and keyboard-visible anchor navigation.
- [x] 3.6 Preserve and integrate the existing ShareBar and footer work without overwriting unrelated user changes.

## 4. Documentation and verification

- [x] 4.1 Update locked product-direction and methodology copy to reflect the three-category public edition, quality-earned volume, non-personalized public brief, evidence roles, and archive gate.
- [x] 4.2 Run the smallest focused shared, auto-publish/claim, API brief, archive, route-registry, and web component tests after their corresponding changes.
- [x] 4.3 Run web/API typechecks, formatting/lint checks for changed files, route/canonical checks, and `pnpm docs:check`.
- [x] 4.4 Inspect the running current and dated briefs at 390, 768, and 1440 pixels; capture after screenshots; run Impeccable critique, polish, audit, and the mechanical detector; resolve every P0/P1 finding.
- [x] 4.5 Record verification evidence and remaining advisory findings in the design receipt, request the owner's keep/close/wrong-lane decision, and run the design-workflow check.
- [x] 4.6 Validate the OpenSpec change strictly and update `PROJECT_STATUS.md` only when the completed local behavior becomes durable current/shipped truth; do not commit, push, deploy, migrate, or archive the change without the corresponding authorization and completion state.
