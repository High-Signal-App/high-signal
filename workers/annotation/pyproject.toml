[project]
name = "high-signal-annotation-worker"
version = "0.1.0"
description = "Cloudflare Python Worker for cheap High Signal source annotation"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = [
  "workers-py",
  "workers-runtime-sdk",
]
