node_modules
.next
.turbo
.wrangler
.open-next
.tmp
dist
out
coverage

.env
.env.local
.env.*.local
.clerk/
apps/web/.clerk/
*.pem
*.key
*.tsbuildinfo
.dev.vars
__pycache__/
*.py[cod]

.DS_Store
*.log
.vscode
.idea

playwright-report
test-results
.vercel

# fallow cache
.fallow/
cache.bin

# Python equities pipeline intermediate (rebuilt by scripts; derived JSON in apps/web/src/data is the tracked artifact)
data/equities-snapshot.jsonl

# CF Pages build output
apps/web/.cf-pages-bundle/

# Local agent-tooling runtime artifacts (session logs/state, not product code)
.commandcode/
.omx/
.symphony/
.clawpatch/

# Generated, regenerable data samples (one query away from source — not a store)
data-directory/

# India D2C opportunity collector artifacts (regenerable by `pnpm d2c:collect`;
# the bundled TS module in packages/shared is the tracked artifact)
data/d2c-opportunities/
data/d2c-agent-visibility/
